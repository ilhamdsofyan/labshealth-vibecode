services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: labshealth-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8002}:8080"
    env_file:
      - .env
    environment:
      PORT: 8080
